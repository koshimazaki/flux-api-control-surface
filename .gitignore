# Dependencies and builds
ui/node_modules/
ui/.next/
ui/out/
ui/*.tsbuildinfo
cloudflare/node_modules/
cloudflare/.wrangler/
cloudflare/.dev.vars

# Python caches
__pycache__/
*.py[cod]
.venv/
venv/

# Local outputs and generated media
/outputs/
/renders/
/samples/
/datasets/
/caption-jobs/
/notes/

# Generated media/model artefacts are local by default. Force-add intentional
# small docs examples explicitly if the public repo needs them.
*.png
*.jpg
*.jpeg
*.webp
*.gif
*.avif
*.mp4
*.mov
*.webm
*.wav
*.mp3
*.flac
*.aiff
*.m4a
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx

# Local/private prompt variants
/prompts/private/
/prompts/local/
/configs/private_*.json
/configs/local_*.json
/configs/personal_*.json
/experiments/**/local/
/experiments/**/outputs/
# Raw experiment batches are private working material; curated results are
# published deliberately, never swept into a commit.
/experiments/

# Soft-deleted prompt recovery archive (local-only)
/configs/deleted_prompts.json

# Secrets and local env
.env
.env.local
*.key
secrets.json
api_keys.txt

# OS/editor noise
.DS_Store
Thumbs.db
*.log
logs/

# FLUX 3 early-access eval material — NEVER commit to this PUBLIC repo
# (beta-confidential: findings, frozen prompts w/ Discord URLs, run records, seeds)
experiments/flux-video-evals/
skills/flux3-archetypes/

# Generated Koshi Lab research dashboards are private working instruments.
wiki-dashboard/

# Nested working clone of this same repo (Codex workspace) — never commit.
/flux-api-control-surface/

# Root-level dependency and output directories (ui/ and cloudflare/ have their own).
/node_modules/
/output/
